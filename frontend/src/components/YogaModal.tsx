import React from 'react';

interface ModalProps {
    name: string;
    description: string;
    images: object;
}

export default function YogaModal({ name, description, images }: ModalProps) {
    return (
        <div>
            
        </div>
    )
    
}